

// const {Command, flags} = require('@oclif/command')

// class BoomCommand extends Command {
//   async run() {

//     await this.config.runHook('telemetry', { data: { info: 'so, this really weird thing happened the other day', link: 'risingj.com' }})

//     this.log('good job')
//     // this.error('boom')
//     // throw new Error('blow up the outside world')
//   }
// }

// module.exports = BoomCommand